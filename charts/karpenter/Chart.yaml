apiVersion: v2
name: karpenter
description: Karpenter node autoscaling for Clever Kubernetes Engine (CKE)
type: application
# Placeholder — the release workflow overrides version/appVersion at package time from the
# git tag, and fails the release if these values lag the tag. Bump both in the release commit.
version: 0.10.0
appVersion: v0.10.0
keywords:
  - karpenter
  - autoscaling
  - clever-cloud
home: https://github.com/CleverCloud/karpenter-provider-clever-cloud
sources:
  - https://github.com/CleverCloud/karpenter-provider-clever-cloud
